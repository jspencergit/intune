[project]
name = "intune-visualizer"
version = "0.2.0"
description = "Real-time pitch visualization and intonation feedback tool for viola (Alto Clef)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyserial>=3.5",
    "numpy>=1.26",
    "PyQt5>=5.15",
    "pyqtgraph>=0.13",
    # matplotlib kept temporarily during transition
    "matplotlib>=3.8",
]

[project.scripts]
intune-visualizer = "visualizer:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["visualizer"]
